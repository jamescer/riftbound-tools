// Re-exports from src so scripts can import the canonical validator and
// valid-value sets without duplicating them.
export { validateCard, validDomains, validTypes, validRarities, validSets } from "../src/utils/validate";
