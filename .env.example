# Riot API key — get yours at https://developer.riotgames.com
# Copy this file to .env and fill in the value.
# .env is gitignored — NEVER commit an actual key.
RIOT_API_KEY=

# Optional: which regional cluster to hit (default: americas).
# Valid values: americas | asia | europe
RIOT_API_REGION=americas
